package com.example.doshka.ui.theme

import androidx.compose.ui.graphics.Color

// Брутальна кольорова палітра Industrial Neo-Brutalism

// Основні кольори
val BrutalOrange = Color(0xFFFF5722)        // Primary — палаюча сталь
val BrutalOrangeDark = Color(0xFFD84315)    // Primary Dark — розпечений метал
val BrutalAmber = Color(0xFFFFB300)         // Secondary — іскри
val BrutalYellow = Color(0xFFFFEB3B)        // Yellow — попередження
val BrutalRed = Color(0xFFFF1744)           // Error — аварія
val BrutalGreen = Color(0xFF00E676)         // Success — зелене світло

// Фони
val BrutalSurfaceLight = Color(0xFFF5F0EB)  // Світла тема — теплий бетон
val BrutalSurfaceDark = Color(0xFF1A1A1A)   // Темна тема — вугілля
val BrutalCardLight = Color(0xFFFFFFFF)     // Картки світла тема
val BrutalCardDark = Color(0xFF242424)      // Картки темна тема

// Текст
val BrutalTextPrimaryLight = Color(0xFF1A1A1A)
val BrutalTextPrimaryDark = Color(0xFFF5F0EB)
val BrutalTextSecondaryLight = Color(0xFF757575)
val BrutalTextSecondaryDark = Color(0xFF9E9E9E)

// Бордери
val BrutalBorderLight = Color(0xFF1A1A1A)
val BrutalBorderDark = Color(0xFFF5F0EB)

// Пріоритети задач
val PriorityCritical = Color(0xFFFF1744)    // Критичний — пульсує
val PriorityHigh = Color(0xFFFF5722)        // Високий
val PriorityMedium = Color(0xFFFFB300)      // Середній
val PriorityLow = Color(0xFF9E9E9E)         // Низький

// Статуси
val StatusTodo = Color(0xFF9E9E9E)          // Очікує
val StatusInProgress = Color(0xFFFFB300)    // В роботі
val StatusReview = Color(0xFF2196F3)        // На перевірці
val StatusDone = Color(0xFF00E676)          // Завершено

// Колонки Kanban
val ColumnBacklog = Color(0xFFE0E0E0)
val ColumnTodo = Color(0xFFBDBDBD)
val ColumnInProgress = Color(0xFFFFE0B2)
val ColumnReview = Color(0xFFBBDEFB)
val ColumnDone = Color(0xFFC8E6C9)

// Тіні (для брутального стилю використовуємо суцільні кольори)
val BrutalShadowLight = Color(0xFF1A1A1A)
val BrutalShadowDark = Color(0xFF000000)
